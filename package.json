{
  "name": "pepe-clocke",
  "version": "1.0.0",
  "description": "Time tracking application",
  "scripts": {
    "deploy": "cd pepe-cls && npm run build && cd .. && npx wrangler pages deploy pepe-cls/dist",
    "build": "cd pepe-cls && npm run build"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}